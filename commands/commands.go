// Package commands provides the built-in CLI commands for klee applications.
//
// Each command constructor takes only the dependencies it needs, keeping
// the package free of framework imports and avoiding circular dependencies.
package commands
